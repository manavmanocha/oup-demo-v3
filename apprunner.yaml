version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - npm ci
      - npm run build

run:
  command: npx vite preview --host 0.0.0.0 --port $PORT
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
    - name: NODE_ENV
      value: production
